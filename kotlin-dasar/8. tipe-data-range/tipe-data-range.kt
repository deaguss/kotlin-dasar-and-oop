fun main() {
    // val range = 0..1000
    // println(range.count()) // Mendapatkan total data di range
    // println(range.contains(2)) // Mengecek apakah terdapat value tersebut
    // println(range.first) // Mendapatkan nilai pertama
    // println(range.last) // Mendapatkan nilai terakhir
    // println(range.step) // Mendapatkan nilai tiap kenaikan

    // val terbalik = 1000 downTo 0 // range terbalik

    val rangeWithStep = 1000 downTo 0 step 10
    println(rangeWithStep.count()) // Mendapatkan total data di range
    println(ranrangeWithStepge.contains(2)) // Mengecek apakah terdapat value tersebut
    println(rangeWithStep.first) // Mendapatkan nilai pertama
    println(rangeWithStep.last) // Mendapatkan nilai terakhir
    println(rangeWithStep.step) // Mendapatkan nilai tiap kenaikan

}