// op + - * / %


fun main() {
    var result: Int = 10 / 2
    var a: Int = 10

    a += result

    println(a)


    // Augmented Assignments
    // a = a + 10 => a += 10

    // Operator Perbandingan
    // > < >= <= == !=
    println(a == result)

    // Operasi Boolean
    // && || !
}